---
import { getEntry, render } from "astro:content";
import SelfHostingPage from "../../components/SelfHostingPage.astro";

const doc = await getEntry("docs", "self-hosting-docker/zh-tw");
if (!doc) {
  throw new Error("Missing Traditional Chinese self-hosting-docker docs entry.");
}

const { Content } = await render(doc);
---

<SelfHostingPage Content={Content} doc={doc} locale="zh-tw" />
